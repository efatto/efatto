<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_invoice_line_form_inherit_purchase" model="ir.ui.view">
        <field name="name">account.invoice.line.form.inherit.purchase</field>
        <field name="model">account.invoice.line</field>
        <field name="inherit_id" ref="account.view_invoice_line_form"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <separator colspan="4" string="Purchase Moves"/>
                <field colspan="4" name="purchase_move_ids" nolabel="1">
                    <tree name="Stock Moves">
                        <field name="date_expected"/>
                        <field name="date"/>
                        <field name="origin"/>
                        <field name="product_id"/>
                        <field name="product_uom_qty"/>
                        <field name="product_uom"/>
                        <field name="state"/>
                        <field name="show_details_visible" invisible="1"/>
                        <button name="action_show_details"
                                string="Register lots, packs, location"
                                type="object" icon="fa-list"
                                attrs="{'invisible': [('show_details_visible', '=', False)]}"
                                options='{"warn": true}'/>
                    </tree>
                </field>
            </field>
        </field>
    </record>
</odoo>

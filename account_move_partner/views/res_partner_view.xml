<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data>

        <record id="view_partner_account_defaults_form" model="ir.ui.view">
            <field name="name">res.partner.account.defaults.form</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <field name="property_payment_term" position="after">
                    <field name="sale_account_ids" domain="[('type','not in',['consolidation', 'closed', 'view'])]" widget="many2many_tags"/>
                </field>
                <field name="property_supplier_payment_term" position="after">
                    <field name="purchase_account_ids" domain="[('type','not in',['consolidation', 'closed', 'view'])]" widget="many2many_tags"/>
                </field>
            </field>
        </record>

  </data>
</openerp>

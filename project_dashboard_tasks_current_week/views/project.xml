<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <record id="view_task_search_form" model="ir.ui.view">
            <field name="model">project.task</field>
            <field name="inherit_id" ref="project.view_task_search_form"/>
            <field name="arch" type="xml">
                <filter name="my_tasks" position="after">
                    <filter string="Current week"
                        name="current_week"
                        domain="[('date_start','&lt;=', (context_today() + relativedelta(weeks=0, weekday=6)).strftime('%%Y-%%m-%%d')),
                        ('date_start','&gt;',(context_today() - relativedelta(weeks=1, weekday=6)).strftime('%%Y-%%m-%%d'))]"
                        help="Display all elements in the current week." />
                </filter>
            </field>
        </record>

        <record id="act_project_project_2_project_task_current_week" model="ir.actions.act_window">
            <field name="name">Tasks</field>
            <field name="res_model">project.task</field>
            <field name="view_mode">calendar,kanban,tree,form,pivot,graph</field>
            <field name="context">{
                'group_by': 'stage_id',
                'search_default_project_id': [active_id],
                'search_default_current_week': True,
                'default_project_id': active_id,
            }</field>
            <field name="search_view_id" ref="view_task_search_form"/>
            <field name="help" type="html">
                <p class="oe_view_nocontent_create">
                    Here, you can create new tasks
                </p>
                <p>
                    You can now manage your tasks in order to get things done efficiently. Track progress, discuss, attach documents, etc.
                </p>
            </field>
        </record>
        <record model="ir.ui.view" id="view_project_kanban">
            <field name="inherit_id" ref="project.view_project_kanban"/>
            <field name="model">project.project</field>
            <field name="arch" type="xml">
                <xpath expr="//templates" position="before">
                    <field name="task_current_week_count"/>
                </xpath>
                 <xpath expr="//div[contains(@class, 'o_project_kanban_boxes')]" position="inside">
                    <div t-if="record.use_tasks.raw_value" class="o_project_kanban_box">
                        <a name="%(act_project_project_2_project_task_current_week)d" type="action">
                            <span class="o_value"><t t-esc="record.task_current_week_count.value"/></span>
                            <span class="o_label">Current Week</span>
                        </a>
                    </div>
                </xpath>
            </field>
        </record>
    </data>
</openerp>